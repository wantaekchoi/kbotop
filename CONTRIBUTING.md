# 기여 안내

작은 프로젝트라 절차는 가볍습니다.

- 버그나 제안은 이슈를 먼저 열어 주세요. 오탈자·문서·명백한 버그는 바로 PR을 열어도 됩니다.
- `cargo test`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt` 세 가지가 통과해야 합니다(CI도 같은 걸 봅니다). 최소 지원 러스트 버전은 `Cargo.toml`의 `rust-version`을 따릅니다.
- 커밋 메시지에는 `feat:` / `fix:` / `docs:` / `chore:` / `test:` 접두사를 씁니다. 변경 이력이 이걸로 정리됩니다.
- 동작이 바뀌는 변경에는 테스트를 함께 넣어 주세요.
- `README.md`를 고쳤으면 `README.en.md`도 맞춰 주세요.

데이터는 네이버 스포츠의 공개(비공식) 엔드포인트에서 옵니다. 필드가 없거나 null이어도 화면이 통째로 깨지지 않게 **관용적으로**, 그리고 **패닉 없이** 파싱하는 것이 원칙입니다.

기여하신 내용은 프로젝트와 같은 [Unlicense](LICENSE)로 배포됩니다.
